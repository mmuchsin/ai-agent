# Example environment variables for development
# Copy this file to .env and replace the placeholder values with real keys.

# OpenRouter (used by `main.py`) - set this to your OpenRouter API key
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Do NOT commit real secrets to source control. Add .env to your .gitignore.
